alert(`Programa para calculo de necessidade de materiais`)

var latao = Number(prompt(`Insira a quantidade de kg de latão que deseja`))
var cobre = latao*0.7
var zinco = latao*0.3
alert(`A quantidade de cobre necessária é ${cobre} kg e a quantiade de zinco é ${zinco} kg`)